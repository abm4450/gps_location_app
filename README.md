# 🔍 كاشف تزوير الموقع - GPS Spoof Detector

تطبيق Flutter متكامل لكشف تزوير الموقع (GPS Spoofing) عبر مقارنة مصادر الموقع المختلفة.

![Flutter](https://img.shields.io/badge/Flutter-3.2+-blue.svg)
![Platform](https://img.shields.io/badge/Platform-iOS%20%7C%20Android-green.svg)

---

## ✨ المميزات

### 🛰️ موقع GPS الأصلي
- تحديد الموقع عبر الأقمار الصناعية مباشرة
- كشف علامة `isMocked` من النظام
- عرض الدقة والارتفاع والسرعة
- تحويل الإحداثيات لعنوان

### 📶 موقع الواي فاي
- تقدير الموقع عبر شبكة WiFi المتصلة
- عرض معلومات الشبكة (SSID, BSSID, IP)
- مقارنة مع موقع GPS

### 📱 موقع الشبكة الخلوية
- تقدير الموقع عبر أبراج الاتصالات
- معلومات الشبكة الخلوية
- أقل دقة لكن مفيد للمقارنة

### 🔍 فحص التزوير الشامل
- مقارنة جميع مصادر الموقع
- كشف Mock Location النشط
- حساب المسافة بين المصادر
- تحليل الدقة المشبوهة
- تقرير مفصل بالمؤشرات

---

## 📊 كيف يعمل كشف التزوير؟

| المؤشر | الوصف | الأهمية |
|--------|-------|---------|
| `isMocked = true` | النظام يشير للتزوير | 🔴 حرج |
| فرق المسافة > 1 كم | GPS بعيد عن WiFi/Cell | 🔴 حرج |
| دقة < 5 متر | دقة مثالية مشبوهة | 🟡 تحذير |
| لا يوجد ارتفاع | GPS الحقيقي يوفره | 🔵 معلومة |
| Developer Options | Mock Location مفعل | 🟡 تحذير |

---

## 🚀 التثبيت

### 1. استنساخ المشروع
```bash
unzip location_spoof_detector.zip
cd gps_location_app
```

### 2. تثبيت المكتبات
```bash
flutter pub get
```

### 3. تشغيل التطبيق
```bash
# Android
flutter run -d android

# iOS
cd ios && pod install && cd ..
flutter run -d ios
```

---

## 📦 بناء التطبيق

### Android APK
```bash
flutter build apk --release
# الملف: build/app/outputs/flutter-apk/app-release.apk
```

### iOS IPA
```bash
flutter build ios --release
open ios/Runner.xcworkspace
# ثم Archive من Xcode
```

---

## 📁 هيكل المشروع

```
lib/
├── main.dart
├── models/
│   ├── location_data.dart      # نموذج الموقع
│   ├── network_data.dart       # نموذج WiFi و Cell
│   └── spoof_result.dart       # نتيجة الفحص
├── services/
│   ├── gps_service.dart        # خدمة GPS
│   ├── wifi_service.dart       # خدمة WiFi
│   ├── cellular_service.dart   # خدمة الخلوي
│   └── spoof_detection_service.dart  # خدمة الكشف
├── screens/
│   └── home_screen.dart        # الشاشة الرئيسية
└── widgets/
    ├── location_card.dart      # بطاقة الموقع
    └── spoof_result_card.dart  # بطاقة النتيجة
```

---

## 📚 المكتبات المستخدمة

| المكتبة | الوظيفة |
|---------|---------|
| `geolocator` | الوصول لـ GPS |
| `geocoding` | تحويل الإحداثيات |
| `network_info_plus` | معلومات WiFi |
| `connectivity_plus` | حالة الاتصال |
| `permission_handler` | إدارة الصلاحيات |
| `device_info_plus` | معلومات الجهاز |

---

## ⚠️ ملاحظات مهمة

### Android
- يتطلب Android 6.0+ (API 23)
- يجب تفعيل GPS للوصول لـ WiFi SSID
- كشف Mock Location يعمل على معظم الأجهزة

### iOS
- يتطلب iOS 12.0+
- لا يمكن الوصول لمعلومات Cell Tower مباشرة
- التزوير أصعب على iOS

---

## 🔒 الصلاحيات المطلوبة

### Android
- `ACCESS_FINE_LOCATION`
- `ACCESS_COARSE_LOCATION`
- `ACCESS_WIFI_STATE`
- `READ_PHONE_STATE`

### iOS
- `NSLocationWhenInUseUsageDescription`
- `NSLocationAlwaysUsageDescription`

---

## 📄 الترخيص

MIT License - مفتوح المصدر
